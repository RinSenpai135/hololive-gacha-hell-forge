# FORGE AND GLOBAL SETTINGS
modLoader="javafml"
loaderVersion="[37,)" 
license="All rights reserved"
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/"

[[mods]]
# The modid of the mod
modId="hololivegachahell"

# The version number of the mod - there's a few well known ${} variables useable here or just hardcode it
# ${file.jarVersion} will substitute the value of the Implementation-Version as read from the mod's JAR file metadata
# see the associated build.gradle script for how to populate this completely automatically during a build
version="${file.jarVersion}"

 # A display name for the mod
displayName="Hololive Gacha Hell"

# A URL to query for updates for this mod.
#updateJSONURL="https://change.me.example.invalid/updates.json"

# A URL for the "homepage" for this mod, displayed in the mod UI
#displayURL="https://github.com/RinSenpai135/hololive-gacha-hell-forge"

# A file name (in the root of the mod JAR) containing a logo for display
logoFile="mod_icon.png"

# A text field displayed in the mod UI
credits="This mod was made thanks to the Hololive talents, who bring joy and ideas across the world!"

# A text field displayed in the mod UI
authors="RinSenpai"

# The description text for the mod (multi line!) (#mandatory)
description='''
This is a simulation of GACHA HELL in Minecraft, with Hololive talents tokens as rewards!
Discover easter eggs and complete the collection!

I am, by no means, affiliated with Mojang Studios or Cover Corp.
This mod doesn't represent the talents nor any parties point of view and is purely made for entertainment purposes.
'''



# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.examplemod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[37,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.examplemod]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.17)"
    ordering="NONE"
    side="BOTH"
